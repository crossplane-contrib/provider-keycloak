apiVersion: group.keycloak.crossplane.io/v1alpha1
kind: Permissions
metadata:
  name: my-group-permission
spec:
  # Fixme: Delete will result in realm not found exception
  managementPolicies: ["Create", "Update", "Observe"]
  forProvider:
    realmIdRef:
      name: "dev"
    groupIdRef:
      name: "test"
  providerConfigRef:
    name: "keycloak-provider-config"
